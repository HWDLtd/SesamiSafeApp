# Auth0 Client IDs are configured per-platform in src/constants/auth0.ts
# (SES-Android-App / SES-iOS-App). Do not put Client Secrets in this app —
# Native applications are public clients and must not ship a client_secret.
